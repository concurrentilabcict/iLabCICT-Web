 export type LoginProps = {
        id: number;
        accessToken: string;
        refreshToken: string;
        name: string;
        email: string;
        role: string;
        profilePicture: string;
    }
